/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  reactStrictMode: true,

  output: "export",
  basePath: "/1mbucks",
  assetPrefix: "/1mbucks/",
};

export default nextConfig;
